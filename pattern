dsa templates

DP TEMPLATE :

this is common pattern for take and non take
recurssion(int index){

	for(int i = index){
 			recurssion(index+1);
	}
}

check meet in middle algo

void dfs(parameters){

    if(base_condition){
        save_answer();
        return;
    }

    for(each choice){

        make_choice();

        dfs(next_state);

        undo_choice();   // backtrack
    }
}

Subsets (LeetCode 78)

Permutations (LeetCode 46)

Combination Sum (LeetCode 39)

N-Queens (LeetCode 51)

Word Search (LeetCode 79)




sliding window / two pointers

template 1 :

int left = 0;

for(int right = 0; right < n; right++){

    // expand window
    add(arr[right]);

    while(window_invalid){
        remove(arr[left]);
        left++;
    }

    update_answer();
}

templat 2:

int left = 0;
int right = arr.length - 1;

while(left < right){

    int sum = arr[left] + arr[right];

    if(sum == target){
        // answer found
        return;
    }

    if(sum < target){
        left++;
    }else{
        right--;
    }
}


this is 2 pointer prefix sum edition imp:

in prefix sum we take overall sum of before that pointer(i)
and try to find the ans

class Solution {
    public int numberOfSubarrays(int[] nums, int k) {

        int n = nums.length;
        int count = 0 ;
        int sum = 0;
        Map<Integer,Integer> map = new HashMap<>();
        
        map.put(0,1);

        for(int i = 0 ;i < n ;i++){

            if(nums[i]%2 != 0)
            sum+=1;

            if(map.containsKey(sum - k)){

                count += map.get(sum-k);
            }

            map.put(sum,map.getOrDefault(sum,0)+1);

        }
        return count;
    }
}


tree : 

🔸 Two Common Conventions (IMPORTANT ⚠️)

There are 2 ways people define height:

1. Height in terms of edges (most used in DSA)
Single node → height = 0
Empty tree → height = -1
2. Height in terms of nodes
Single node → height = 1
Empty tree → height = 0

👉 Always clarify in interview!


public int height(TreeNode root) {
    if (root == null) return -1; // for edge-based

    int left = height(root.left);
    int right = height(root.right);

    return 1 + Math.max(left, right);
}
this code is by counting edges 

bfs will be having : sc o(width)
dfs will be having :sc o(height)
tc will be o(n) for both

tree imp concepts :

balanced binary tree = height of left subtree - height of right sub tree <= 1
diameter = height of left subtree + height of right sub tree


graph:

dfs in ajc matrix
 void dfs(boolean[] isVisited, int[][] isConnected , int node){

        isVisited[node] = true;

        for(int nei = 0 ;nei<isConnected.length;nei++){

            if(isConnected[node][nei] == 1 && !isVisited[nei] )
            dfs(isVisited,isConnected,nei);

        }
    }


comman pattern for bfs in array:

take q put into q 

int[][] directions = {{1,0},{0,1},{-1,0},{0,-1}}; 
while(!q.isEmpty())
{
int[] coordinates = q.poll();
   for(int[] direction : directions){
    
            int r = coordinates[0]+direction[0];
            int c = coordinates[1]+direction[1];
            //do any stuff on r and c
    
   }
}

stack

next greater , next smaller 

